<Window x:Class="NtErp.Shell.Demo.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NtErp.Shell.Demo.Views"        
        xmlns:prism="http://prismlibrary.com/"
        xmlns:region="clr-namespace:NtErp.Shared.Services.Regions;assembly=NtErp.Shared.Services"
        mc:Ignorable="d"
        Title="Shell" Height="700" Width="950"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Stretch">
    <Grid>
        <Menu prism:RegionManager.RegionName="{x:Static region:RegionNames.ShellMenuBar}"
              Height="20" VerticalAlignment="Top">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" />
            </MenuItem>
        </Menu>
        <ContentControl prism:RegionManager.RegionName="{x:Static region:RegionNames.MainContent}"
                        Margin="0,25,0,30" />
        <StatusBar prism:RegionManager.RegionName="{x:Static region:RegionNames.StatusBar}"
                   Height="30" VerticalAlignment="Bottom">            
        </StatusBar>
    </Grid>
</Window>
